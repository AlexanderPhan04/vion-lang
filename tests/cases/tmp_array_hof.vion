let arr = [1, 2, 3, 4, 5]

let doubled = arr_map(arr, fn(x, i) {
    return x * 2
})
print doubled

let evens = arr_filter(arr, fn(x, i) {
    return x % 2 == 0
})
print evens

let sum = arr_reduce(arr, fn(acc, curr, i) {
    return acc + curr
}, 0)
print sum
